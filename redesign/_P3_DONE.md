V2 done — dashboard env tiles re-skinned to .env-tile frame (glow/actions/env-stat/pills); real data only (Accounts+Updated), all data-* hooks + checkProxy output span preserved; skeleton reshaped.
V3 done — sidebar folder nodes + account rows + multi-select re-skinned to DS (.account-btn.is-active rail, .avatar, .pill--ltd, .row-actions, .btn btn-icon-sm btn-ghost); all data-* + delegation class hooks + balance tri-state + patchSidebarBalances preserved; kept legacy checkbox-sibling structure; dropped data-less session dot (logged CHANGES.md); P-042–P-066 ticked; node --check clean.
V4 done — account header (.pill proxy/local + Full/LTD tier pill, .btn btn-secondary/btn-primary btn-sm), category tabs → .chip aria-pressed toggles, item table → .items-wrap/.items-table (index.html shell + renderItemRow .rar bar/.item-icon/is-selected/thSort-thPlain-thCheck); windowing math + all data-*/id/delegation hooks + tri-state + pricing-honesty value cell preserved verbatim; P-186–P-203, P-272–P-306 ticked; node --check clean; index.html tag-balanced.
V5 done — fleet masters (folder/selection/env/global master headers + global env-filter). All four master #main-header clusters → masterpiece pattern: icon-less .truncate title + .pill pill--brand scope badge + .btn <variant> btn-sm actions (Mass Buy=btn-buy, Delete=btn-danger, rest=btn-secondary); renderGlobalFilter → .surface strip of .chip aria-pressed toggles. Every id hook (btn-folder-massbuy/-bans/-refresh-folder, btn-sel-massbuy/-refresh/-move/-bans/-delete, sel-all/sel-clear-all, btn-env-bans, btn-global-bans/-refresh-global) + data-genv + all listener wiring preserved verbatim; zero handler/API/logic change (diff = markup only). Decorative fa title icons dropped to match masterpiece (logged CHANGES). P-169–185, P-256–266 ticked; node --check clean.
V6 done — inventory header/refresh. Game toggle → masterpiece .seg control with .is-on active button (index.html) + updateGameToggle re-wired from className-overwrite to classList.toggle('is-on') (ids #btn-game-cs2/#btn-game-tf2 + setGame handlers unchanged); breadcrumb → chevron separators + brand-light active (renderBreadcrumb; data-bc hooks + nav actions unchanged); src/currency split control kept as the custom DS-tokened wrapper (dropdown popovers need it — .seg has overflow:hidden; #src-logo/#src-menu/#cur-menu/data-src/data-cur unchanged); refresh-progress + refresh-failed panels already DS in the V1 shell. Group F P-086–109 ticked; node --check clean; index.html tag-balanced (250/250 div).
V7 done — value-history chart (renderHistoryChart). SVG already used .hist-* DS classes + --brand/--success tokens; two focused changes: (1) chart TIME AXIS fmtTime de-DE→en-GB (English-only invariant 8 / P4-align; de-DE stays ONLY for EUR money per ST-02), (2) legend markers → masterpiece slim bar swatches (colors match each line's stroke: items=--brand, wallet=--success) with fmtCents values. Shared-money-scale math, area fill, last-point dots, partial-wallet dashing + "(incomplete)" honesty marker + point count all preserved verbatim (pricing honesty S2/S13). #history-chart/#history-legend mounts unchanged. Group L P-161–168 ticked; node --check clean.
V9 done 💰 — Global Trade-Offers manager (two-sided sent/received, batch accept/decline/cancel). MARKUP-ONLY: app.js offerStateBadge → .pill pill--* variants (warn/listed/success/danger/neutral), offerRowActions → .btn btn-sm (Accept=emerald, Cancel/Decline=btn-secondary+danger color), offersRowHtml badge span → .pill; index.html offers-overlay shell chrome (header .t16/.field/.modal-x + #offers-scope .pill pill--brand + all 6 batch buttons → .btn btn-sm). ZERO handler change: loadOffers/onSingleOfferAction/batchOffers (ssimConfirm gates + /api/trade/offers-batch + 2FA "awaiting mobile confirmation" honesty)/removeOfferRow/bindOffersControls/search all byte-identical. Preserved every hook: #offers-scope/-search/-refresh/-close/-sent-*/-recv-* (18 ids), .offer-row/.offer-check/.offer-act, data-offer-action/-id/-active/-search/-username. offerSideThumbs real icons kept (richer than prototype count-pills). Group Q P-225–255 ticked; node --check clean; div 250/250; handler-safety diff = 1 comment.
V8 done 💰 — Active Orders view (sell listings + buy orders + single/bulk cancel). MARKUP-ONLY re-skin of ordersShellHtml (toolbar+sections → .surface/.panel-head/.panel-title/.field/.btn btn-sm), cancelBtn (.order-cancel btn btn-sm + --danger-rgb), buyOrderRow/sellOrderRow (t13/t10 type scale), loading spinner accent→brand. ZERO handler/API/confirm change: bindOrdersControls, bulkCancelOrders (ssimConfirm gate), cancelOrder (ssimConfirm gate), removeOrderRow, updateOrdersSelCount, checked/visibleOrderRows all byte-identical. Preserved every hook: #orders-search/-cancel-selected/-sel-count/-cancel-all/-refresh, .order-row/.order-check/.order-cancel, data-order-kind/-id/-name, data-cancel-buy/-listing. CRITICAL: kept removeOrderRow's load-bearing DOM (rows-list → previousElementSibling=.panel-head → span.font-mono count). Partial-snapshot amber banner unchanged (pricing honesty). Group P P-204–224 ticked; node --check clean; handler-safety diff = only 2 comment lines.
V11 done 💰 — folder mass-buy modal (buy one item across a whole folder). MARKUP-ONLY. index.html #fbuy-overlay shell → masterpiece DS: header .t16 + .modal-x close (#fbuy-close id kept — handler binds by id), summary → .t12, labels → .field-label, Game/Price/Search inputs+selects → .field, Market-price → .btn btn-secondary btn-sm, amber note → .t11 rounded-xl, progress panel (#fbuy-bar → bg-brand, #fbuy-phase/#fbuy-count → t10, #fbuy-end → .btn btn-danger btn-sm), Close → .btn btn-secondary, Start mass buy → .btn btn-buy. app.js renderFbuySearch dropdown rows → .t13/.t10 + hover teal→brand (hover:bg-brand/15) (data-i + click-fill addEventListener block untouched); renderFolderBuyResults rows → t13/t10 + status .pill pill--success/listed/neutral/danger (refresh-failed keeps distinct triangle icon; message title honesty preserved). submitFolderBuy 2-phase pre-buy refresh + all handlers byte-identical (pollFolderBuy, searchFbuyItems, fetchFbuyPrice, openFolderBuy, closeFolderBuy, hideFbuySearch); ssimConfirm spend gate intact. Hooks preserved (17 ids: fbuy-overlay/-close/-cancel/-form/-submit/-summary/-game/-name/-name-results/-price/-price-fetch/-progress/-phase/-bar/-count/-end/-results + data-i). P-477–483 ticked; node --check clean; div 250/250.
V10 done 💰 — market-buy modal (single Steam buy-order). MARKUP-ONLY. index.html #buy-overlay shell → masterpiece DS: header .t16 title + .modal-x close (#buy-close id kept — handler binds by id), .field-label labels, .field inputs+selects, Max/Market-price → .btn btn-secondary btn-sm, Buy submit → .btn btn-buy, Cancel → .btn btn-secondary, wallet/total/amber note → .t11, rounded-lg→rounded-xl. app.js renderBuySearch dropdown rows → .t13/.t10 type scale + hover teal→brand (hover:bg-brand/15); its addEventListener click-fill block untouched. ZERO handler change: submitBuy (SACRED buy re-POST / createBuyOrder finalize), updateBuyWallet, refreshBuyWallet, recomputeBuyTotal, fillMaxBuyQty, fetchBuyPrice, searchBuyItems, buyCurrencyCode, closeBuyModal all byte-identical. renderBuyWallet + openBuyModal left byte-identical (textContent/`<option>` only — no DS markup to swap); wallet line re-skinned on its #buy-wallet host. Balance tri-state honesty (unknown / 0,00 / value) preserved. Hooks preserved: buy-overlay/-close/-form/-submit/-account/-account-list/-wallet/-game/-qty/-max/-qty-echo/-name/-name-results/-price/-price-fetch/-cur/-total/-result/-cancel + data-i. P-466–476 ticked; node --check clean; div 250/250.
V12 done 💰 — sell modal (mass-sell on Steam market). MARKUP-ONLY. index.html #sell-overlay shell → masterpiece DS: header .t16 + .modal-x close (#sell-close/#sell-cancel ids KEPT — handlers bind by id, not data-close), summary → .t14 rounded-xl, "Sale price" → .field-label, strategy radio cards → rounded-xl/.t14/.t11 (peer-checked emerald states kept), custom-price input → .field w-32, Calculate button → .btn btn-secondary btn-sm w-full, preview-result → rounded-xl .t12, amber 2FA/irreversible note → .t11 rounded-xl, Cancel → .btn btn-secondary, Sell&confirm → .btn btn-sell; #sell-progress panel → bar bg-emerald→bg-brand, #sell-end → .btn btn-danger btn-sm (inner byte-identical to resetEndBtn re-emit), .t10/.t12/.t14 type scale. app.js renderSellPreview → header .t10 / table .t13, unpriced "no price" → .pill pill--danger (kept VISUALLY DISTINCT from priced emerald-mono = pricing honesty inv.5), reprice btn → .btn btn-icon-sm btn-secondary (retryOnePrice only swaps innerHTML → className persists), missing note → .t10; totals/priced rows + all fmtEurCents math byte-identical. submitSell (+ ssimConfirm spend gate) / previewSell / pollSell / selectedSellItems / sellStrategy / customSellCents / closeSellModal / toggleSellCustomRow / openSellModal / showSellProgress byte-identical (no api/fetch/ssimConfirm/state line touched). Hooks preserved (16 ids: sell-overlay/-close/-form/-summary/-from/-custom-row/-custom-price/-preview-btn/-preview-result/-cancel/-submit/-progress/-count/-bar/-detail/-end + 3 name=sellstrategy radios + data-reprice). P-441–451 ticked; node --check clean; div 250/250.
V13 done 💰 — send-trade modal (single account + folder mass-send). MARKUP-ONLY. index.html #trade-overlay shell → masterpiece DS: header .t16 + .modal-x close (#trade-close id kept — handler binds by id), summary box → rounded-xl .t14, internal/external radio cards → rounded-xl .t14 (peer-checked brand states kept), env/folder selects + recipient search → .field (search keeps .field pl-9 + magnifier), external trade-url → .field, labels → .field-label, recipient-list wrapper rounded-lg→rounded-xl, empty/count lines → .t11, amber 2FA note → .t11 rounded-xl, Cancel → .btn btn-secondary, Send&confirm → .btn btn-primary (brand; inner byte-identical to setButtonLoading re-emit). #mass-progress panel → t14/t12/t10, bar bg-brand (already), #mass-end → .btn btn-danger btn-sm (inner byte-identical to resetEndBtn re-emit). app.js recipientRow re-skinned → .avatar/.t13/.t10 (data-recip hook + selected brand-ring + check icon preserved). submitTrade/submitMassTrade/pollMass/closeTradeModal/showMassProgress/openTradeModal/populateTradeFolders/buildRecipientList/readTradeTarget/updateTradeTargetVisibility/surfaceTradeFailures byte-identical (no api/fetch/ssimConfirm/addEventListener/setTimeout/state line touched); submitTrade send exec + /api/trade/send POST + INV-E1 9s re-refresh + End-task ssimConfirm gate UNCHANGED. Hooks preserved (17 ids: trade-overlay/-close/-cancel/-form/-submit/-summary/-from/-internal-block/-external-block/-target-url/-env/-folder/-search/-recipient-list/-recipient-empty/-recipient-count + mass-progress/-bar/-count/-detail/-end + data-recip + name=target radios). P-426–437 ticked; node --check clean; div 250/250.
