SSIM v1.5.4

+ Every account now logs in as its own PC. They all reported the same hostname before.
+ Price fetching starts on its own. If nothing is logged in it brings a few accounts up, one per proxy to maximize speed.
+ Buy order and top-up billing details now stay the same for each account, instead of being redrawn on every order.

- The CSFloat auto-accept log no longer repeats the same line every 45 seconds when nothing has changed.
